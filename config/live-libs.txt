# pk's OS :: extra shared libs always needed in the live image
# (ldd does not list them directly - they come via dlopen/pthread paths)
# one line = one lib (the real file found on the host gets copied)
libgcc_s.so.1
libatomic.so.1
libstdc++.so.6
