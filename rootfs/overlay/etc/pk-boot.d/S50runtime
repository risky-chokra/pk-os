#!/bin/sh
# boot hook: attach the app runtime (/opt/pk) - for Linux/Windows apps
# boot option to disable:  pk_runtime=off
[ -x /sbin/pk-runtime ] || exit 0
mkdir -p /run/pk
/sbin/pk-runtime start > /run/pk/runtime.log 2>&1 &
exit 0
